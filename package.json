{
  "name": "filtered-github-api-meta-generator",
  "version": "1.0.0",
  "description": "Generate a filtered list of CIDR blocks from the GitHub API Meta URL specifically formatted for a PaloAlto Firewall",
  "main": "index.js",
  "scripts": {
    "process": "index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Matt Finholt-Daniel",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0"
  }
}
